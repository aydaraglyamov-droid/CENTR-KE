{
  "name": "centr-ke",
  "version": "1.0.0",
  "description": "Казан Егетләре — Аналитик Центр",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 8000",
    "dev": "vercel dev",
    "install": "echo 'Dependencies installed'",
    "test": "echo 'No tests configured'"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
